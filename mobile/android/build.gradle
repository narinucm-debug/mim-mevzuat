buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.5.0'
    }
}

// Proje seviyesi repository'ler settings.gradle'daki
// dependencyResolutionManagement bloğunda merkezi olarak tanımlı
// (FAIL_ON_PROJECT_REPOS) - burada tekrar tanımlamak Gradle'ı
// "InvalidUserCodeException: prefer settings repositories" hatasıyla
// durdurur.
